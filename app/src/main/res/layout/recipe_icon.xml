<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="160dp"
    android:layout_height="160dp"
    android:padding="5dp"
    >
    <!-- BACKDROP THUMBNAIL -->
    <ImageView
        android:id="@+id/recipe_icon_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:background="@drawable/thumbnail"/>

    <!-- RECIPE TITLE -->
    <TextView
        android:id="@+id/recipe_icon_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/recipe_icon_image"
        android:layout_alignRight="@id/recipe_icon_image"

        android:layout_marginTop="5dp"
        android:paddingTop="5px"
        android:paddingBottom="5px"
        android:gravity="center"

        android:text="RECIPE NAME"
        android:textColor="#FFF"
        android:background="#9000"
        >


    </TextView>

    <!-- FAVOURITES -->
    <TextView
        android:id="@+id/recipe_icon_numfavourites"
        android:gravity="bottom"
        android:textSize="12dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawableEnd="@drawable/heart_off"
        android:text="0"
        android:textColor="#FFF"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:paddingStart="4dp"
        android:paddingEnd="2dp"
        android:drawablePadding="2dp"
        android:layout_margin="5dp"
        android:layout_alignBottom="@id/recipe_icon_image"
        android:layout_alignEnd="@id/recipe_icon_image"
        android:background="#9000"
        />


    <!-- FAVOURITES -->
    <TextView
        android:textSize="12dp"
        android:gravity="bottom"
        android:id="@+id/recipe_icon_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawableStart="@drawable/heart_off"
        android:text="0 mins"
        android:textColor="#FFF"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:paddingStart="4dp"
        android:paddingEnd="2dp"
        android:drawablePadding="2dp"
        android:layout_margin="5dp"
        android:layout_alignBottom="@id/recipe_icon_image"
        android:layout_alignStart="@id/recipe_icon_image"
        android:background="#9000"
        />


</RelativeLayout>
